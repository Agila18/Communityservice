# Limitations and Future Directions

## 1. Current System Limitations

While *Vanakkam Akka* fundamentally shifts the paradigm of rural maternal healthcare tracking natively, it operates within structural barriers securely bound natively.
* **No Direct Hardware Vitals Integration:** Currently, parameters like Hemoglobin, Blood Pressure, and pulse rate rely exclusively on manual subjective input/OCR document structures implicitly. Without integrated bluetooth pulse oximetry or BP cuffs, exact deterministic analysis fundamentally limits prediction capabilities gracefully.
* **Lack of Universal EMR Standardization:** Tamil Nadu’s core PHC pipelines still rely on disparate databases structurally tracking information diversely preventing absolute clean mapping gracefully down natively onto a single unified backend interface. 
* **Network Reliability:** While the `OfflineSyncManager` masks latency gracefully natively via strict queue logic dynamically resolving state, an absolute multi-week internet blackout still locally caps complex AI generative abstraction inherently until sync establishes cleanly.

## 2. Phase 2 Features Roadmap

**1. Audio-Based Emotional Biomarker Tracking:** Integrating acoustic signal processing analyzing pitch/prosody inside the VoiceChat natively identifying microscopic emotional markers accurately preceding Postpartum Depression globally without requiring complex semantic structures.
**2. Smart Device Integration Tracker:** Specifically partnering with localized Anganwadi (Childcare) centers deploying localized edge-BP endpoints transmitting BLE structures dynamically back to the user's phone gracefully bypassing manual entry logic heavily. 
**3. Peer-To-Peer Mesh Synchronization:** Utilizing WiFi-Direct / Bluetooth Mesh pipelines inherently leaping offline packets seamlessly through local community chains fundamentally reaching internet nodes securely without SIM cards locally.

## 3. Potential for Scale

*Vanakkam Akka* is structurally abstracted completely off pure Tamil logic. The application architecture isolating distinct String Dictionaries enables uniquely cheap scaling models:
* **Odisha (Odia) / Bihar (Bhojpuri):** Mirroring the specific NLP Dialect Matrix dynamically opens the 40-Week mapping directly into heavy maternal demographic centers dynamically across India inherently utilizing identical Offline-first syncing loops globally.
* **Global South Applicability:** Architecturally the same barriers (Poor GUI literacy, Low 2G networks, Intimidating Clinical workflows) exist deeply inside Sub-Saharan pipelines inherently mapping identical value securely solving identical demographic limits fundamentally.
